# FishGo - AI Fish Species Classification App

> Indonesian marine fish species classification using EfficientNet-B0 + TensorFlow Lite

## Project Overview

FishGo는 인도네시아 해양 어류 19종을 분류하는 Android 앱입니다.
Navigo 앱에 통합될 물고기 인식 기능의 독립 MVP 버전입니다.

### Design Philosophy
- **정확도 우선**: 1-2초 추론 시간 허용
- **오프라인 동작**: 모델 내장, 네트워크 불필요
- **저사양 지원**: RAM 2GB+, Android SDK 24+

### Tech Stack
- **Model**: EfficientNet-B0 -> TFLite FP16
- **Android**: Kotlin, CameraX, TensorFlow Lite Interpreter
- **Build**: Gradle 8.4, GitHub Actions CI/CD

## Quick Links

- [Model Documentation](./model.md) - 모델 학습/변환 정보
- [Development Log](./log.md) - 개발 이력 및 이슈 해결
- [Task Specification](./tasks20251223.md) - Classification 앱 개발 명세

## Project Structure

```
fishgo/
├── app/
│   ├── src/main/
│   │   ├── assets/model/          # TFLite 모델 및 레이블
│   │   │   ├── fish_classifier.tflite
│   │   │   └── labels.txt
│   │   ├── java/.../fishgo/
│   │   │   ├── MainActivity.kt     # 카메라 + 가이드 프레임
│   │   │   ├── ResultActivity.kt   # 분류 결과 화면
│   │   │   ├── classifier/
│   │   │   │   ├── FishClassifier.kt       # TFLite 분류
│   │   │   │   └── ClassificationResult.kt # 결과 데이터
│   │   │   └── ui/
│   │   │       └── FishGuideOverlay.kt     # 가이드 프레임 오버레이
│   │   └── res/
│   │       ├── layout/
│   │       │   ├── activity_main.xml
│   │       │   └── activity_result.xml
│   │       └── mipmap-*/ic_launcher.png  # 앱 아이콘
│   └── build.gradle.kts
├── scripts/                        # 모델 학습/변환 스크립트
│   ├── prepare_classification_data.py
│   ├── train_classifier.py
│   └── convert_to_tflite.py
├── gradle/
│   ├── wrapper/
│   └── libs.versions.toml         # 의존성 버전 관리
├── .github/workflows/
│   └── android-build.yml          # CI/CD 빌드
├── docs/                          # 문서
├── fishgo01.png                   # 인앱 로고
├── fishgo02.png                   # 앱 아이콘 원본
└── README.md
```

## App Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    MainActivity                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                 Camera Preview                           ││
│  │  ┌───────────────────────────────────────────────────┐  ││
│  │  │                                                   │  ││
│  │  │  ╔═══════════════════════════════════════════╗   │  ││
│  │  │  ║                                           ║   │  ││
│  │  │  ║         가이드 프레임 (3:1 비율)          ║   │  ││
│  │  │  ║         물고기를 이 안에 맞추세요         ║   │  ││
│  │  │  ║                                           ║   │  ││
│  │  │  ╚═══════════════════════════════════════════╝   │  ││
│  │  │                                                   │  ││
│  │  └───────────────────────────────────────────────────┘  ││
│  │                                                         ││
│  │                  [  📷 Ambil Foto  ]                    ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ 촬영 후
┌─────────────────────────────────────────────────────────────┐
│                    ResultActivity                            │
│  ┌─────────────────────────────────────────────────────────┐│
│  │               [촬영된 이미지]                           ││
│  └─────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────┐│
│  │                       🐟                                ││
│  │                    Tongkol                              ││
│  │              Euthynnus Affinis                          ││
│  │                                                         ││
│  │  Akurasi: ████████████████░░░  94%                      ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
│         [ Foto Ulang ]        [ Simpan ]                    │
└─────────────────────────────────────────────────────────────┘
```

## Key Classes

### FishClassifier.kt
EfficientNet-B0 TFLite 모델을 사용한 물고기 분류 클래스.

```kotlin
class FishClassifier(context: Context) {
    fun classify(bitmap: Bitmap): ClassificationResult
    fun close()
}
```

**중요 구현 사항:**
- TFLite Core Interpreter 사용
- 입력 크기: 224x224 RGB
- ImageNet 정규화: mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]
- CPU 전용 (4 threads)

### MainActivity.kt
카메라 프리뷰 + 가이드 프레임 + 촬영 버튼.

**동작 흐름:**
1. 카메라 권한 요청
2. CameraX Preview 시작
3. 가이드 프레임 오버레이 표시
4. 촬영 버튼 클릭
5. 가이드 영역 crop -> 정사각형 패딩 -> ResultActivity로 전달

### ResultActivity.kt
분류 결과 표시 화면.

**표시 정보:**
- 인도네시아어 이름 (크게)
- 학명 (작게, 이탤릭)
- 신뢰도 (프로그레스 바 + 퍼센트)
- 경고 메시지 (낮은 신뢰도 시)

## Build & Deploy

### GitHub Actions
- Push to `main` -> 자동 빌드
- Artifacts: `fishgo-debug`, `fishgo-release-unsigned`

## Dependencies

| Library | Version | Purpose |
|---------|---------|---------|
| TensorFlow Lite | 2.14.0 | 모델 추론 |
| TensorFlow Lite Support | 0.4.4 | 이미지 처리 |
| CameraX | 1.3.1 | 카메라 프리뷰 |
| Material | 1.11.0 | UI 컴포넌트 |

## Model Training

모델 학습 스크립트는 `scripts/` 디렉토리에 있습니다:

```bash
# 1. 데이터셋 준비 (YOLO Detection -> Classification)
python scripts/prepare_classification_data.py

# 2. EfficientNet-B0 학습
python scripts/train_classifier.py

# 3. TFLite 변환
python scripts/convert_to_tflite.py
```

자세한 내용은 [Model Documentation](./model.md) 참조.

## Notes for Future Development

### Navigo 통합 시 고려사항
1. `FishClassifier` 클래스를 그대로 사용 가능
2. `ClassificationResult` 데이터 클래스 활용
3. UI는 Navigo 디자인에 맞게 수정 필요

### 알려진 제한사항
- GPU 가속 미지원 (버전 충돌로 제거)
- 19종 어류만 분류 가능
- 오프라인 전용 (서버 API 없음)
